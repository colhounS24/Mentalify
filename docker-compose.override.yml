# docker compose file for local development
services:
  frontend:
    image: fyp24jd002/frontend:dev
    build:
      target: dev
      context: mentalify
      dockerfile: docker/Dockerfile
    ports:
      - "5173:5173"
